{
  "name": "baitdash-monorepo",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.3",
    "typescript": "^5.5.4"
  }
}
